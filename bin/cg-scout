#!/usr/bin/env ruby

require 'optparse'
require 'methadone'
require 'cg_scout.rb'
require 'version.rb'

class App
  include Methadone::Main
  include Methadone::CLILogging

  main do 
    # check if working folder is a cg app and a git repo
    # fetch all environment possibilities from `deploy_dir/environments`
    # check if provided environment is a match

  end

  on("-e ENV", "--environment", "Specific environment to check")

  version CgScout::VERSION

  description "See what's deployed where"

  go!
end
